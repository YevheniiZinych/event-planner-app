import { NavLink } from "react-router-dom";
// import { useLocation } from "react-router";
// import sprite from "styled-components";

const Details = () => {
  return (
    <div>
      <NavLink>Back</NavLink>
    </div>
  );
};

export default Details;
